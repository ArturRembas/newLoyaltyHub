package it.unicam.cs.ids.LoyaltyHub.controller;

import it.unicam.cs.ids.LoyaltyHub.model.ActivityAdmin;

/**
 * Controller interface for {@link ActivityAdmin} entities.
 * Extends the {@link EntityController} interface to provide CRUD operations and potentially other custom functionalities specific to {@link ActivityAdmin} entities.
 * This interface is intended to be implemented by controllers that handle HTTP requests for {@link ActivityAdmin} entities.
 */
public interface ActivityAdminController extends EntityController<ActivityAdmin, Long> {
    // Additional methods specific to ActivityAdmin entities can be defined here
}
