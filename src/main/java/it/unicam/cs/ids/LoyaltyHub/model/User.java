package it.unicam.cs.ids.LoyaltyHub.model;

/**
 * Represents a generic user in the LoyaltyHub system.
 * This interface serves as a base for different types of users within the system,
 * allowing for future expansion and the addition of specific user roles.
 */
public interface User {
    
}
