package it.unicam.cs.ids.LoyaltyHub.service;

import it.unicam.cs.ids.LoyaltyHub.model.ActivityAdmin;

/**
 * Management interface for {@link ActivityAdmin} entities.
 * Extends the {@link EntityManager} interface to provide specific management operations for {@link ActivityAdmin} entities.
 * This interface can be extended to include custom management operations related to {@link ActivityAdmin} entities.
 */
public interface ActivityAdminManager extends EntityManager<ActivityAdmin, Long> {
    
}
